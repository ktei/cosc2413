// Generated by CoffeeScript 1.6.3
(function() {
  $(function() {
    $('input.quantity').val(1);
    $('button.decrease').click(function() {
      var currentQuantity, input, newQuantity;
      input = $(this).parent().siblings('input.quantity');
      currentQuantity = input.val();
      newQuantity = parseInt(currentQuantity) - 1;
      if (newQuantity < 1) {
        newQuantity = 1;
      }
      return input.val(newQuantity);
    });
    return $('button.increase').click(function() {
      var currentQuantity, input, newQuantity;
      input = $(this).parent().siblings('input.quantity');
      currentQuantity = input.val();
      newQuantity = parseInt(currentQuantity) + 1;
      return input.val(newQuantity);
    });
  });

}).call(this);
